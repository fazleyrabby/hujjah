# Hujjah — Handover Prompt (2026-04-30)

## What is Hujjah

Free, open-source Islamic reference app (offline-first). Two targets:
- **`apps/web`** — Next.js web app (primary, deployed at https://hujjah.fazleyrabbi.xyz). **Priority.**
- **`apps/tauri`** — Tauri desktop app (secondary, same UI codebase). Fix after web is stable.

Both share two SQLite databases (symlinked from tauri → web):
- `hujjah-quran.db` — Quran, translations, tafsir, embeddings
- `hujjah-hadith-core.db` — Hadiths, translations, narrators, chains, embeddings

---

## Current DB State

### Hadith DB (`hujjah-hadith-core.db`)
| Metric | Value |
|---|---|
| Total hadiths | 53,986 |
| EN translations (`sunnah.com`) | 42,512 |
| BN translations (`hadith-api` / fawazahmed0) | 24,655 |
| AI translations (`qwen3.5-9b`) | 11,597 |
| Narrator chain fetch progress | ~9,500 (as of 2026-04-30) |
| `hadith_narrators` | populated after chain import reaches Kutub al-Sittah |
| Narrators | 24,184 |

**Key translator field names** (use these, not `github-classic`):
- English: `translator = 'sunnah.com'`, `lang_code = 'en'`
- Bengali: `translator = 'hadith-api'`, `lang_code = 'bn'`
- AI: `translator = 'qwen3.5-9b'`

### Quran DB (`hujjah-quran.db`)
| Metric | Value |
|---|---|
| Verses | 6,236 |
| Translations | 68,596 |
| Tafsir rows | 18,689 (3 slugs) |
| Tafsir slugs | `en-ibn-kathir`, `bn-ibn-kathir`, `bn-abu-bakr-zakaria` |

---

## What Was Done (2026-04-30)

### 1. SettingsDropdown Component
Added `apps/web/components/SettingsDropdown.tsx` — integrated into all pages:
- `/` (home), `/hadith`, `/about`, `/settings`, `/chain`
- Controls: language, font size, Arabic font (Scheherazade New / KFGQPC Uthmani HAFS), dark mode
- Dropdown inline in page header alongside AppNav

### 2. Arabic Font Switching Fixed
- Added `@font-face` for `KFGQPC Uthmani Script HAFS` in `globals.css` (woff2 in `public/fonts/`)
- Scheherazade New loaded via Google Fonts in `layout.tsx`
- `THEME_SCRIPT` in layout now applies `--font-arabic` on page load (before React hydration)
- React `useEffect` in page.tsx also applies font change on switch

### 3. Quran/Hadith Tab State Persistence
- Added `lastSelectedSurah` state — tracks last viewed surah
- On initial load: restore from `localStorage.getItem("hujjah-surah")` or default to Al-Fatiha (1)
- Switching to hadith: clears surahVerses + selectedSurah
- Switching back to quran: restores last selected surah
- Selected surah persisted to `localStorage.setItem("hujjah-surah")` on load

### 4. Chain Page Navigation
- Added AppNav + SettingsDropdown to `/chain` page (was previously showing maintenance with no nav)
- Both web and tauri `/chain` pages now have proper navigation

### 5. Type Fixes
Fixed type errors across all pages from SettingsDropdown prop types:
- `onFontSizeChange`: `(size: string) => void` → cast to `'small' | 'medium' | 'large'`
- `setLang` in settings: `string` → `as 'en' | 'bn'`
- `stopAudio` click handler: wrapped in `() => stopAudio()` (takes optional boolean param)

---

## What Is Left to Implement

### HIGH PRIORITY

**1. Narrator chain data (auto-running)**
- Script: `apps/web/scripts/sunnah-migration/11-fetch-sunnah-chains.py`
- Rate: 3 req/sec, 4,700/day cap
- Cron: `0 9 * * *` — resumes automatically from `narrator_fetch_progress` table
- ~9 more days until it reaches Bukhari/Muslim which have narrator tags
- When `hadith_narrators` is populated → re-enable chain/graph pages

**2. Re-enable chain explorer**
When narrator data is ready:
- Remove maintenance divs from `/chain` and `/chain/graph` in both apps
- Restore content from git: `git show HEAD~1:apps/web/app/chain/page.tsx`
- Consider adding reliability badges using islamweb narrator data if available

**3. BN hadith translations gap**
- Only 24,655 BN hadiths vs 42,512 EN. ihadis.com was contacted — awaiting reply.
- Script ready: `apps/web/scripts/sunnah-migration/13-scrape-ihadis-bn.py` (paused)

### MEDIUM PRIORITY

**4. Islamweb.net narrator bios**
- Email sent to islamweb.net (draft at `docs/email-islamweb-data-partnership.md`)
- Scraper ready: `apps/web/scripts/sunnah-migration/15-scrape-islamweb-narrators.py`
- **Do NOT run until they reply**

**5. Tauri app parity**
- Web is primary; Tauri was updated alongside but not tested
- After web is stable, run Tauri dev server and verify same features work
- Known: Tauri reads SQLite directly (no API routes), uses `lib/db.ts` and `lib/hadith-db.ts`

### LOW PRIORITY / FUTURE

**6. More tafsir slugs**
Currently: `en-ibn-kathir`, `bn-ibn-kathir`, `bn-abu-bakr-zakaria`
Could add: `ar-ibn-kathir`, `ar-tabari`, `en-maariful-quran` via quran.com API

**7. AR language support**
Arabic UI partially stubbed in chain pages. Would need:
- Arabic translations (none in DB currently)
- RTL layout pass
- Arabic narrator search

**8. Embedding search for hadiths**
BGE-M3 embeddings done (AR: 53,984, EN: 42,512, BN: 24,655).
Semantic search endpoint exists but not surfaced in UI beyond AI chat.

---

## Key File Map

```
apps/web/
  app/
    page.tsx              — Home (Quran + hadith tab + tafsir)
    hadith/page.tsx       — Hadith browse/search
    chain/page.tsx        — Chain Explorer (nav + dropdown added, still shows maintenance)
    chain/graph/page.tsx  — Graph/tree view
    about/page.tsx        — Data sources
    settings/page.tsx     — Settings
    api/
      quran/route.ts      — Quran API (tafsir, search, verse)
      hadith/route.ts     — Hadith API (books, search, byBook, random)
      chain/route.ts      — Chain/narrator API
  components/
    SettingsDropdown.tsx  — Inline settings (lang, font size, arabic font, dark mode)
  lib/
    web-db.ts             — SQLite connection for web (better-sqlite3)
  contexts/
    AudioContext.tsx      — Quran audio playback state

apps/tauri/
  lib/
    db.ts                 — Quran SQLite (tafsir, translations)
    hadith-db.ts          — Hadith SQLite (translations use sunnah.com/hadith-api)
    chain-db.ts           — Chain/narrator SQLite
    ai/explain.ts         — AI explain pipeline

apps/web/scripts/sunnah-migration/
  11-fetch-sunnah-chains.py   — Sunnah.com narrator chain fetch (cron, active)
  12-import-tafsir.py         — Tafsir import from quran.com
  13-scrape-ihadis-bn.py      — BN translation scraper (paused, awaiting reply)
  15-scrape-islamweb-narrators.py — Narrator bio scraper (DO NOT RUN YET)

docs/
  email-islamweb-data-partnership.md — Email draft to islamweb.net
  handover-2026-04-29.md             — Previous handover (2026-04-29)
  session-notes-2026-04-28.md
  session-notes-2026-04-29.md
```

---

## Deploy

Web app deployed via `./scripts/deploy-web.sh`:
- Builds Docker image (linux/amd64)
- Transfers to VPS via SSH
- Runs via docker compose
- Cloudflare tunnel for `hujjah.fazleyrabbi.xyz`

Deploy command: `./scripts/deploy-web.sh`

---

## Immediate Next Step

Monitor chain fetch progress — ~9 days until it reaches Bukhari/Muslim. Once narrator tags are in DB, re-enable chain explorer UI.